/* calculate the euclidean distance between two points */
double dist(double x0, double y0, double x1, double y1);

/* generate an image of cocentric circles with the offests and size specified */
void draw_waves(int side_length, int x_offset, int y_offset);

/* genereate an image of cocentric circles with offsets and size specified */
void cmds_to_draw(int argc, char** argv);